# derived.env — Computed variables (derived from deployment.env)
# Versioned. Uses variable interpolation from earlier env files.

# Full URLs for services
CLIENT_URL=https://${FRONTEND_DOMAIN}
LANDING_URL=https://${DOMAIN_NAME}
TEMPLATES_URL=https://${TEMPLATES_DOMAIN}
STORAGE_URL=https://${STORAGE_DOMAIN}
